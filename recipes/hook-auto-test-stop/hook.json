{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -f package.json ] && grep -q '\"test\"' package.json; then npm test --if-present 2>&1 | tail -20; else echo 'no package.json or test script — skipping'; fi"
          }
        ]
      }
    ]
  }
}
